<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinClientApp.Views.Barang_Edit">
  <ScrollView>
  <StackLayout>
    <Label Text="ID :"/>
    <Entry x:Name="txtIdBarang" IsEnabled="False" Text="{Binding IdBarang}"/>
    <!-- Input - NamaBarang -->
    <Label Text="Nama Barang :"/>
    <Entry x:Name="txtNamaBarang" Text="{Binding NamaBarang}"/>
    <!-- Input - HargaBarang -->
    <Label Text="Harga :"/>
    <Entry x:Name="txtHargaBarang" Text="{Binding HargaBarang}"/>
    <!-- Input - StokBarang -->
    <Label Text="Stok :"/>
    <Entry x:Name="txtStokBarang" Text="{Binding StokBarang}"/>
    <!-- Input - TglBeli -->
    <Label Text="Tanggal Beli :"/>
    <Entry x:Name="txtTglBeli" Text="{Binding TglBeli}"/>
    <!-- Input - IdKategori -->
    <Label Text="Kategori:"/>
    <Entry x:Name="txtIdKategori" Text="{Binding IdKategori}"/>
    <!-- Input - IdJenisMotor -->
    <Label Text="Jenis Motor:"/>
    <Entry x:Name="txtIdJenisMotor" Text="{Binding IdJenisMotor}"/>
    <!-- Submit -->
    <StackLayout Orientation="Horizontal">
      <Button x:Name="btnEdit" Text="Edit"/>
      <Button x:Name="btnDelete" Text="Delete"/>
    </StackLayout>
  </StackLayout>
  </ScrollView>
</ContentPage>